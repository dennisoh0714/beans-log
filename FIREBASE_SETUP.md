# 카페 데니스 관리자 로그인 설정

이번 index.html은 하드코딩된 관리자 비밀번호를 제거하고 Firebase 이메일/비밀번호 로그인과 `/admins/{uid}` 권한 확인을 사용합니다. 기존 원두, 로고, 영어 이름, 요청 데이터 경로는 유지합니다. 기존 비밀번호는 더 이상 작동하지 않습니다.

## 소유자가 한 번 설정할 항목

1. https://console.firebase.google.com/project/dennis-beans-log/authentication 에서 이메일/비밀번호 로그인 공급자를 활성화합니다.
2. Authentication → Users → Add user에서 관리자 이메일과 **새로운 비밀번호**로 계정을 만듭니다. 비밀번호는 GitHub나 채팅에 올리지 않습니다. 기존 공개 비밀번호를 재사용하지 않습니다.
3. 만들어진 사용자 UID를 복사합니다.
4. Realtime Database → Data에서 먼저 데이터 전체를 JSON으로 내보내 백업합니다. 루트에 `admins` 노드를 추가하고 그 아래에 복사한 UID를 키, **boolean true**를 값으로 추가합니다. 예: `/admins/복사한UID = true`. `cafeDennis` 내부가 아닌 루트의 형제 노드입니다. 전체 데이터 JSON을 덮어쓰지 마세요.
5. Realtime Database → Rules에서 이 저장소의 `database.rules.json` 내용을 붙여 넣고 Publish를 누릅니다. 이 규칙은 이 사이트가 사용하는 경로만 허용합니다. 같은 데이터베이스에 다른 앱이 있다면 기존 규칙을 백업하고 해당 경로의 접근 요구사항을 함께 검토해야 합니다.
6. 사이트의 관리자 탭에서 새 이메일과 비밀번호로 로그인합니다. 원두 수정/저장, 로그아웃, 일반 방문자 투표와 요청 제출을 확인합니다.

## 규칙의 역할

- 방문자는 원두/로고/영어 이름/원두 요청을 읽을 수 있습니다. 원두 요청은 기존처럼 공개 데이터이므로 개인정보를 넣지 않습니다.
- 원두/로고 변경과 요청 삭제·정리는 `/admins/{uid} = true`인 계정만 가능합니다.
- 웹 클라이언트는 관리자 권한을 직접 부여할 수 없습니다. Firebase 콘솔에서만 관리자 UID를 관리합니다.
- 방문자 투표는 기존 기능을 유지하되 특정 원두의 투표 수를 1씩 증감하는 쓰기만 허용합니다. 요청은 신규 항목 추가와 필드 검증만 허용합니다.
- 로그인하지 않은 방문자의 투표/요청은 신원별 중복 방지나 서버 속도 제한을 제공하지 않습니다. 브라우저의 기존 중복/간격 제한은 보안 경계가 아닙니다. 강한 남용 방지에는 별도 인증·App Check·서버 처리가 필요합니다.

## 적용 상태와 확인

GitHub에 규칙 파일을 올리는 것만으로 Firebase 규칙이 배포되지는 않습니다. 콘솔에서 위 설정을 완료하기 전에는 관리자 로그인이 되지 않을 수 있으며, 기존 데이터베이스 규칙의 보안 상태도 바뀌지 않습니다. HTML의 로그인 화면만으로 데이터베이스를 보호할 수는 없습니다.

이 변경은 소스에서 비밀번호를 없애지만 과거 Git 커밋의 비밀번호까지 지우지는 않습니다. 새 비밀번호와 서버 접근 규칙을 반드시 사용합니다.

공식 문서:
- https://firebase.google.com/docs/auth/web/password-auth
- https://firebase.google.com/docs/database/security/rules-conditions
