{
  "rules": {
    ".read": false,
    ".write": false,
    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },
    "cafeDennis": {
      "beans": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",
        "$bean": {
          "votes": {
            ".write": "data.parent().exists() && newData.isNumber() && newData.val() >= 0 && ((!data.exists() && newData.val() === 1) || (data.isNumber() && (newData.val() === data.val() + 1 || newData.val() === data.val() - 1 || (data.val() === 0 && newData.val() === 0))))",
            ".validate": "newData.isNumber() && newData.val() >= 0"
          }
        }
      },
      "brandLogos": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
      },
      "brandNamesEn": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
      },
      "requests": {
        ".read": true,
        ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",
        "$request": {
          ".write": "!data.exists() && newData.exists()",
          ".validate": "newData.hasChildren(['id','country','process','brand','comment','ts'])",
          "id": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "country": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "process": {
            ".validate": "newData.isString() && newData.val().length <= 100"
          },
          "brand": {
            ".validate": "newData.isString() && newData.val().length <= 40"
          },
          "comment": {
            ".validate": "newData.isString() && newData.val().length <= 120"
          },
          "ts": {
            ".validate": "newData.isNumber() && ((auth != null && root.child('admins').child(auth.uid).val() === true) || (newData.val() <= now + 60000 && newData.val() >= now - 300000))"
          },
          "$other": {
            ".validate": false
          }
        }
      }
    }
  }
}
